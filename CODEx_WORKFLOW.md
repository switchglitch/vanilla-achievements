# Codex Workflow

Use these paths for every change in this project:

- Source repo root: `C:\Users\Mike\Documents\Codex\Vanilla Achievements`
- Git remote: `https://github.com/switchglitch/vanilla-achievements.git`
- Live addon folder: `C:\Games\Octowow\Interface\AddOns\VanillaAchievements`

Rules for every update:

1. Open the source repo root above first.
2. Make and test the change in the repo root.
3. Copy the updated addon files into the live addon folder above.
4. Verify the live addon copy matches the repo for changed files.
5. Commit and push only after the live addon folder is synced.

If there is any mismatch between the repo and the live addon folder, fix the live addon folder before pushing.
